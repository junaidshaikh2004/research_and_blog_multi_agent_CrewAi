[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.17",
    "django-cors-headers>=4.9.0",
    "fpdf2>=2.8.8",
    "gunicorn>=26.2.0",
    "python-dotenv>=1.2.3",
    "research_and_blog_crew",
]

[tool.uv.sources]
research_and_blog_crew = { path = "../crewai_agent", editable = true }
